-- For startup run 'npm run build' and 'npm start' in seperate terminals.

-- For testing run 'npm test'.

-- For sample data for the database, run 'npm run seed'.

-- add " --collectCoverage" to test script for test coverage